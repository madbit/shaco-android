<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".RegistrationConfirmCodeActivity" >

    <TextView
        android:id="@+id/reg_code_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/label_reg_code_countrycode" />

    <EditText
        android:id="@+id/reg_code_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/reg_code_label"
        android:layout_marginTop="16dp"        
        android:inputType="number"
        android:ems="10" >
        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/reg_code_button_confirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/reg_code_text"
        android:layout_marginTop="28dp"
        android:text="@string/button_reg_code_confirm"
        android:onClick="confirm" />

</RelativeLayout>